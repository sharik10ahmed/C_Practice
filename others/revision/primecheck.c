#include<stdio.h>

int main(){
    int n,sum=0,num,Avg;
    scanf("%d",&num);
    sum=sum+num;
    n=n-1;
    if (n=0)
    {
       printf("\n",Avg=sum/n);
    }
   else
   {
    printf("");
   }
    return 0;
}