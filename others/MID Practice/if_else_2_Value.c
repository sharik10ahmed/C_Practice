#include<stdio.h>
int main(){
    int n1=4,n2=7;
    if(n1>n2){    printf("First No. is larger than Second = %d",n1);
    }
    else{
    printf("Second No. is larger than First = %d",n2);
    }
    return 0;
}
